
{
  "name": "playwright-api-framework",
  "version": "1.0.0",
  "description": "API testing framework using Playwright and JavaScript with DummyJSON API",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
